# AGENTS.md

## Cursor Cloud specific instructions

This repository (`forexanalysis`) is currently an empty skeleton with only a `README.md`. There is no application code, no dependencies, no tests, and no services to run.

### Available runtimes on the VM

- Node.js v22 (via nvm)
- Python 3.12
- Git 2.43

### Notes for future agents

- When application code is added, update this file with setup instructions, test commands, and service startup notes.
- No update script dependencies exist yet. The update script is a no-op until code is added.
